## 🗓️ 2026-04-13 — Keep local dev artifacts out of git

Hey! Here is what changed today in this project:

### What's New
Nothing user-facing today — this is a housekeeping update for the repo itself. The goal is to keep your working tree clean and make sure only real product changes get committed.

### What Was Improved
We now ignore a `.superpowers/` folder that can be generated by local tooling during development. This prevents accidental commits of machine-specific files (like local server PID/state) that don’t belong in the repo and can cause needless diffs.

### What Was Removed or Cleaned Up
Cleaned up previously generated `.superpowers/` artifacts that were sitting in the working tree. They’re not part of the app, and they would just add noise to future commits.

### Files That Changed
- `.gitignore` — ignore `.superpowers/` so local tool output doesn’t get committed

### Why This Matters
It keeps the repository focused on actual application work, avoids “mystery diffs” from local tooling, and makes reviews and deployments less error-prone.

